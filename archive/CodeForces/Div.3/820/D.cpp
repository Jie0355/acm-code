#include <bits/stdc++.h>
#define IOS ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
using namespace std;
const int M = 1e5 + 10;
int main() {
    IOS
    int t;
    cin >> t;
    while (t --) {
        int n;
        int x[M], y[M]; // cost & budget
        cin >> n;
        for (int i = 0; i < n; i ++) cin >> x[i];
        for (int i = 0; i < n; i ++) cin >> y[i];
        for (int i = 0; i < n; i ++) {
            
        }
    }
    return 0;
}