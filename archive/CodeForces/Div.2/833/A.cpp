#include <bits/stdc++.h>
#define IOS ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
using namespace std;
typedef long long LL;
int main() {
    IOS
    int t;
    cin >> t;
    while (t --) {
        LL n;
        cin >> n;
        cout << (n + 1) / 2 << '\n';
    }
    return 0;
}