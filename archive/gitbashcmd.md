```
git remote -v
cd v*
cd ..
```

```
touch filename.filetype
re filename.filetype
mkdir dirname
re -r dirname
```

```
git add -A
git commit -m ""
git commit --amend -m ""
git push origin master
git push origin master -f
git log
```

```
git pull origin
git fetch + git merge
git clone https
```