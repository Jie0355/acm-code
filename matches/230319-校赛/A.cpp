#include <bits/stdc++.h>

using namespace std;
using i64 = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int x;
    cin >> x;
    if (x >= 0) cout << x << '\n';
    else cout << 0 << '\n';

    return 0;
}