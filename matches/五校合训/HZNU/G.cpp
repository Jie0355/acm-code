#include <bits/stdc++.h>

using namespace std;
using i64 = long long;

const int M = 2e5 + 10;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    i64 n, m, b;
    cin >> n >> m >> b;

    i64 arr[M];
    for (int i = 0; i < n; i ++) cin >> arr[i];

    

    return 0;
}